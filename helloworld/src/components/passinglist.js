import React from 'react'

function passinglist({person}) {
    return (
        <div>
            <div>I am {person.name} and age is {person.age}</div>
        </div>
    )
}
export default passinglist
